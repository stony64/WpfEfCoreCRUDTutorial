<Window x:Class="WpfEfCoreCRUDTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:WpfEfCoreCRUDTutorial.ViewModels"
        mc:Ignorable="d"
        Title="EF Core 9 CRUD Demo"
        Height="500"
        Width="700">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Eingabe -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="0,0,0,20">
            <Label Content="Name:"
                   Width="80"
                   VerticalAlignment="Center" />
            <!-- TextBox bindet an ViewModel-Property Name -->
            <TextBox Width="220"
                     Margin="0,0,15,0"
                     VerticalAlignment="Center"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <Label Content="Email:"
                   Width="80"
                   VerticalAlignment="Center" />
            <!-- TextBox bindet an ViewModel-Property Email -->
            <TextBox Width="220"
                     VerticalAlignment="Center"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- CRUD Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,20">
            <!-- Buttons rufen Commands im ViewModel auf -->
            <Button Content="➕ Neu"
                    Margin="0,0,10,0"
                    Command="{Binding CreateCommand}" />

            <Button Content="📋 Alle laden"
                    Margin="0,0,10,0"
                    Command="{Binding LoadCommand}" />

            <Button Content="✏️ Update"
                    Margin="0,0,10,0"
                    Command="{Binding UpdateCommand}" />

            <Button Content="🗑️ Löschen"
                    Command="{Binding DeleteCommand}" />
        </StackPanel>

        <!-- Liste -->
        <ListBox Grid.Row="2"
                 Margin="0,0,0,20"
                 ItemsSource="{Binding People}"
                 SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                 SelectionMode="Single"
                 IsSynchronizedWithCurrentItem="True">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                                Margin="5">
                        <TextBlock Text="{Binding Id}"
                                   Width="50"
                                   FontWeight="Bold" />
                        <TextBlock Text=" | "
                                   Margin="5,0" />
                        <TextBlock Text="{Binding Name}"
                                   Width="180"
                                   Margin="5,0" />
                        <TextBlock Text="{Binding Email, StringFormat=' | {0}'}"
                                   Width="220" />
                        <TextBlock Text="{Binding CreatedAt, StringFormat=' | {0:dd.MM HH:mm}'}"
                                   Foreground="Gray"
                                   FontSize="11" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status -->
        <StatusBar Grid.Row="3">
            <!-- Bindet an StatusMessage aus dem ViewModel -->
            <StatusBarItem Content="{Binding StatusMessage}" />
        </StatusBar>
    </Grid>
</Window>
